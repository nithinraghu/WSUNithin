package statistics;

public class ServerStats {
	
	//Write a constructor
	
	//Fields required - ServerId, TimeStamp, CpuPercentage, DiskPercentage, NetworkPercentage. 
	
	//Write getter and setter methods for all the above fields 
	
	//As updates happen on the Server object, we create a ServerStats object at that time stamp (You will require syncAllStatus())
	
	//Extra credit - If you can get this object saved in the database (You will need setters/getters for the fields)

}
